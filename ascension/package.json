{
  "name": "ascension",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "ASCENSION — a personal transformation operating system, built for one person: Russ.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:single": "npm run build && node scripts/build-single.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^5.0.3"
  },
  "devDependencies": {
    "@types/react": "^18.3.20",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^4.5.0",
    "typescript": "^5.7.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.4"
  }
}
